# Theme
source "~/.config/tmux/tokyonight.tmux"

# Using space as leader key
unbind C-space
set -g prefix C-Space
bind C-Space send-prefix

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
